<main>
  <div class="custom-container my-0 p-2">
        <div class="custom-box p-4 my-2 mx-0">
          <h3 class="mb-4"><i class="bi bi-pencil-square me-2"></i>Editar Galeria de Fotos, Áudios e Vídeos</h3>

          <!-- Tabs -->
          <ul class="nav nav-tabs fw-semibold" id="editGalleryTabs" role="tablist">
            <li class="nav-item" role="presentation">
              <a class="nav-link active text-dark" id="tab-fotos" data-bs-toggle="tab" href="#fotos" role="tab">📷 Fotos</a>
            </li>
            <li class="nav-item" role="presentation">
              <a class="nav-link text-dark" id="tab-audios" data-bs-toggle="tab" href="#audios" role="tab">🎧 Áudios</a>
            </li>
            <li class="nav-item" role="presentation">
              <a class="nav-link text-dark" id="tab-videos" data-bs-toggle="tab" href="#videos" role="tab">🎬 Vídeos</a>
            </li>
          </ul>

          <div class="tab-content mt-3" id="editGalleryTabsContent">

            <!-- Fotos -->
            <div class="tab-pane fade show active" id="fotos" role="tabpanel">
              <form action="/memorial/{{slug}}/gallery/update/photo" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" accept="image/*" class="form-control my-2" required>
                <input type="hidden" name="slug" value="{{slug}}">
                <input type="hidden" name="memorialId" value="{{memorialId}}">
                <input type="hidden" name="userId" value="{{userId}}">
                <div class="text-end">
                  <button type="submit" class="btn btn-primary rounded-2 shadow-sm px-4 py-2">Enviar</button>
                </div>
              </form>

              <div class="row gallery-grid mt-3">
                {{#if gallery.photos.length}}
                  {{#each gallery.photos}}
                    <div class="col-sm-6 col-md-4 col-lg-3 mb-4 position-relative">
                      <a href="/memorials/{{../slug}}/photos/{{this.filename}}" target="_blank">
                        <img src="/memorials/{{../slug}}/photos/{{this.filename}}" class="galeria-foto rounded shadow-sm img-fluid" alt="Foto do memorial {{../slug}}">
                      </a>
                      <form action="/memorial/{{../slug}}/gallery/delete/photo" method="POST" class="delete-form">
                        <input type="hidden" name="fileName" value="{{this.filename}}" />
                        <button type="submit" class="btn btn-sm btn-danger position-absolute top-0 end-0 m-1 shadow-sm">
                          <i class="bi bi-trash-fill"></i>
                        </button>
                      </form>
                    </div>
                  {{/each}}
                {{else}}
                  <p class="text-muted">Nenhuma foto disponível.</p>
                {{/if}}
              </div>
            </div>

            <!-- Áudios -->
            <div class="tab-pane fade" id="audios" role="tabpanel">
              <form action="/memorial/{{slug}}/gallery/update/audio" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" accept="audio/*" class="form-control my-2" required>
                <input type="hidden" name="slug" value="{{slug}}">
                <input type="hidden" name="memorialId" value="{{memorialId}}">
                <input type="hidden" name="userId" value="{{userId}}">
                <div class="text-end">
                  <button type="submit" class="btn btn-primary rounded-2 shadow-sm px-4 py-2">Enviar</button>
                </div>
              </form>

              <div class="row mt-3">
                {{#if gallery.audios.length}}
                  {{#each gallery.audios}}
                    <div class="col-md-6 mb-3 position-relative">
                      <audio controls class="media-player w-100 border rounded shadow-sm p-2">
                        <source src="/memorials/{{../slug}}/audios/{{this.filename}}" type="audio/mpeg" />
                        Seu navegador não suporta áudio.
                      </audio>
                      <form action="/memorial/{{../slug}}/gallery/delete/audio" method="POST" class="delete-form">
                        <input type="hidden" name="fileName" value="{{this.filename}}" />
                        <button type="submit" class="btn btn-sm btn-danger position-absolute top-0 end-0 m-1 shadow-sm">
                          <i class="bi bi-trash-fill"></i>
                        </button>
                      </form>
                    </div>
                  {{/each}}
                {{else}}
                  <p class="text-muted">Nenhum áudio disponível.</p>
                {{/if}}
              </div>
            </div>

            <!-- Vídeos -->
            <div class="tab-pane fade" id="videos" role="tabpanel">
              <form action="/memorial/{{slug}}/gallery/update/video" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" accept="video/*" class="form-control my-2" required>
                <input type="hidden" name="slug" value="{{slug}}">
                <input type="hidden" name="memorialId" value="{{memorialId}}">
                <input type="hidden" name="userId" value="{{userId}}">
                <div class="text-end">
                  <button type="submit" class="btn btn-primary rounded-2 shadow-sm px-4 py-2">Enviar</button>
                </div>
              </form>

              <div class="row mt-3">
                {{#if gallery.videos.length}}
                  {{#each gallery.videos}}
                    <div class="col-md-6 mb-4 position-relative">
                      <video controls class="media-player w-100 rounded shadow-sm">
                        <source src="/memorials/{{../slug}}/videos/{{this.filename}}" type="video/mp4" />
                        Seu navegador não suporta vídeos.
                      </video>
                      <form action="/memorial/{{../slug}}/gallery/delete/video" method="POST" class="delete-form">
                        <input type="hidden" name="fileName" value="{{this.filename}}" />
                        <button type="submit" class="btn btn-sm btn-danger position-absolute top-0 end-0 m-1 shadow-sm">
                          <i class="bi bi-trash-fill"></i>
                        </button>
                      </form>
                    </div>
                  {{/each}}
                {{else}}
                  <p class="text-muted">Nenhum vídeo disponível.</p>
                {{/if}}
              </div>
            </div>

          </div>
        </div>
    

     

    
  </div>
</main>
