<main>
  <!-- Caixa principal -->
  <div class="custom-box p-3 my-2 mx-0">

    <!-- Título -->
    <h3 class="mb-3">
      <i class="bi bi-collection me-2"></i>
      Galeria de Fotos, Áudios e Vídeos
    </h3>

    <!-- Tabs -->
    <ul class="nav nav-tabs fw-semibold" id="myTab" role="tablist">
      <li class="nav-item">
        <a
          class="nav-link active text-dark"
          data-bs-toggle="tab"
          href="#fotos"
          role="tab"
        >
          📷 Fotos
        </a>
      </li>
      <li class="nav-item">
        <a
          class="nav-link text-dark"
          data-bs-toggle="tab"
          href="#audios"
          role="tab"
        >
          🎧 Áudios
        </a>
      </li>
      <li class="nav-item">
        <a
          class="nav-link text-dark"
          data-bs-toggle="tab"
          href="#videos"
          role="tab"
        >
          🎬 Vídeos
        </a>
      </li>
    </ul>

    <!-- Conteúdo das abas -->
    <div class="tab-content mt-3" id="myTabContent">

      <!-- ============================
           📸 FOTOS
      ============================ -->
      <div class="tab-pane fade show active" id="fotos" role="tabpanel">
        <div class="card p-3">

          <!-- Grid de fotos -->
          <div class="row gallery-grid">
            {{#if gallery.photos.length}}
              {{#each gallery.photos}}
                <div class="col-12 col-md-6 mb-4">
                  <a href="{{this.url}}" target="_blank">
                    <img
                      src="{{this.url}}"
                      class="galeria-foto rounded shadow-sm img-fluid"
                      alt="Foto do memorial"
                    />
                  </a>
                </div>
              {{/each}}
            {{else}}
              <p class="text-muted">Nenhuma foto disponível.</p>
            {{/if}}
          </div>

          <!-- Botão Editar Galeria -->
          <div class="d-flex justify-content-end mt-3">
            <a
              href="/memorial/{{slug}}/gallery/edit/{{id}}"
              class="btn-default-memorial"
            >
              <i class="bi bi-pencil-square me-2"></i>Editar Galeria
            </a>
          </div>
        </div>
      </di

      <!-- ============================
           🎧 ÁUDIOS
      ============================ -->
      <div class="tab-pane fade" id="audios" role="tabpanel">
        <div class="card p-3">
          <div class="row">
            {{#if gallery.audios.length}}
              {{#each gallery.audios}}
                <div class="col-md-6 mb-3">
                  <audio
                    controls
                    class="media-player w-100 border rounded shadow-sm p-2"
                  >
                    <source src="{{this.url}}" type="audio/mpeg" />
                    Seu navegador não suporta áudio.
                  </audio>
                </div>
              {{/each}}
            {{else}}
              <p class="text-muted">Nenhum áudio disponível.</p>
            {{/if}}
          </div>

          <!-- Botão -->
          <div class="d-flex justify-content-end mt-3">
            <a
              href="/memorial/{{slug}}/gallery/edit/{{id}}"
              class="btn-edit-outline"
            >
              <i class="bi bi-pencil-square me-2"></i>Editar Galeria
            </a>
          </div>

        </div>
      </div>

      <!-- ============================
           🎬 VÍDEOS
      ============================ -->
      <div class="tab-pane fade" id="videos" role="tabpanel">
        <div class="card p-3">
          <div class="row">
            {{#if gallery.videos.length}}
              {{#each gallery.videos}}
                <div class="col-md-6 mb-4">
                  <video controls class="media-player w-100 rounded shadow-sm">
                    <source src="{{this.url}}" type="video/mp4" />
                    Seu navegador não suporta vídeos.
                  </video>
                </div>
              {{/each}}
            {{else}}
              <p class="text-muted">Nenhum vídeo disponível.</p>
            {{/if}}
          </div>

          <!-- Botão -->
          <div class="d-flex justify-content-end mt-3">
            <a
              href="/memorial/{{slug}}/gallery/edit/{{id}}"
              class="btn-edit-outline"
            >
              <i class="bi bi-pencil-square me-2"></i>Editar Galeria
            </a>
          </div>

        </div>
      </div>

    </div>
  </div>
</main>