<div class="container-sm mt-4">
  <div class="row justify-content-center">
    <div class="col-6">
      <div class="card border-0 shadow-sm">
        <div class="card-body">
          <h4 class="card-title mb-4 text-center">📊 Estatísticas Gerais</h4>
          <div class="vstack gap-3">
            <div class="d-flex justify-content-between border-bottom pb-2">
              <strong>🧾 Memoriais Criados</strong>
              <span>{{totalMemoriais}}</span>
            </div>
            <div class="d-flex justify-content-between border-bottom pb-2">
              <strong>💐 Tributos Postados</strong>
              <span>{{totalTributos}}</span>
            </div>
            <div class="d-flex justify-content-between border-bottom pb-2">
              <strong>📖 Histórias de Vida</strong>
              <span>{{totalHistorias}}</span>
            </div>
            <div class="d-flex justify-content-between border-bottom pb-2">
              <strong>🤝 Histórias Compartilhadas</strong>
              <span>{{totalCompartilhadas}}</span>
            </div>
            <div class="d-flex justify-content-between">
              <strong>👁️ Total de Visitas</strong>
              <span>{{totalVisitas}}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>